{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && composer install"
  },
  "deploy": {
"startCommand": "php artisan config:clear && php artisan storage:link && php artisan migrate --force --seed && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE"
  }
}
